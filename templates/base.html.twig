<!DOCTYPE html>
<html style="overflow-x:hidden">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <nav class="navbar navbar-expand-lg navbar-light bg-info ">
            <a class="navbar-brand text-white" href="#" style="padding-left: 1em">MegaCasting</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/Accueil">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="#">Interview</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="#">Fiche métier</a>
                    </li>



                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li class="nav-item">
                            <a class="nav-link text-white" href="/profil">Profil</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link text-white" href="/login">Profil</a>
                        </li>
                    {% endif %}
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li class="nav-item">
                            <a class="nav-link text-white" href="/logout">Se déconnecter</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link text-white" href="/login">Se connecter</a>
                        </li>
                    {% endif %}


                </ul>
            </div>
            <div class="d-flex justify-content-end mt-2 me-5 mb-2">
                <ul class="navbar-nav">
                    <li class="nav-item ">
                        <form class="form-inline d-flex justify-content-end" action="{{ path('app_search') }}" name="query">
                            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search" name="id">
                            <button class="btn btn-light my-2 my-sm-0 " type="submit">Search</button>
                        </form>
                    </li>
                </ul>
            </div>
        </nav>

        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
